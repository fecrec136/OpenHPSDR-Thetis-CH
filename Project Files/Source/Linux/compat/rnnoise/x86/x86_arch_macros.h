/* Linux stand-in for rnnoise's src/x86/x86_arch_macros.h (not bundled).
   Upstream this header only translates MSVC /arch settings into the
   __SSE__/__AVX__ style macros that GCC and Clang already define. */
#ifndef X86_ARCH_MACROS_H
#define X86_ARCH_MACROS_H
#endif
